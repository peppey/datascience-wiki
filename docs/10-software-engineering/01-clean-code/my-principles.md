# My Clean Code Principles

## Story

The code should read like a story. Start with the main() function, give each function and variable a descriptive name (Even if they get very long).
The function calls in main() should also read like a story.

Functions should not have more than one function. Also do not nest functions more than strictly necessary.

Use descriptive and standardized docstrings. Inline comments should not be necessary.
