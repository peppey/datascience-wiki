# REST APIs

## REST API Principles

REST APIs typically follow these principles:
- resources instead of actions
- HTTP methods define operations
- stateless communication
- standardized representations (usually JSON)

Example:
```text
GET    /models
POST   /models
GET    /models/{id}
DELETE /models/{id}
```
